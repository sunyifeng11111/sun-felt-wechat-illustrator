---
name: sun-felt-wechat-illustrator
description: 为中文微信公众号文章分析内容并生成清晰易懂、真实 3D 毛毡质感且图文一体的头条长封面、1:1 分享页封面与正文配图，同时给出每张正文图应插在哪个原句之后。用于用户提供文章、Markdown 或正文，并要求“公众号封面”“分享封面”“方形封面”“毛毡配图”“文章插图”“为正文配图”“只做封面”“生成 N 张配图”“推荐配图数量”等任务；未提供文章标题时默认根据核心命题生成合适的封面文字，正文图未指定画幅时默认 16:9，也支持用户明确指定 4:3 或 3:4，并使用少量毛毡文字帮助单图理解；适配方法论、教程、专业干货、商业科技、情感叙事、个人成长、生活方式、教育与母婴等内容。
---

# 3D 毛毡公众号配图

## 核心原则

按以下顺序做决定，不得颠倒：

1. 读者能否迅速看懂；
2. 图是否忠实完成当前段落的读者任务；
3. 必要文字是否逐字正确并真实存在于画面中；
4. 画面是否匹配文章体裁、情绪与严肃程度；
5. 构图是否简洁、有层级；
6. 标题与标签是否像真实毛毡物件并参与共同构图；
7. 毛毡材质是否可信且全组一致。

毛毡是表现材质，不是信息结构。优先使用正文里的具体人物、动作、物件和全球通用符号；禁止为了独特而发明晦涩装置。

### 默认交付契约

用户未明确缩小范围时，整套视觉默认包含：

1. 1 张约 `2.35:1` 的公众号头条长封面；
2. 1 张 `1:1` 的分享页封面；
3. 推荐数量的正文配图，默认 `16:9`；用户明确指定时可使用 `4:3` 或 `3:4`；
4. 每张正文图的精确插入提示，格式为“将 `{文件名}` 放在「{原文完整句子}」之后。”

除非用户明确要求对应封面无文字，否则两种封面都必须有正确的毛毡标题。文章没有原标题时，根据核心命题和读者承诺生成忠实、简洁的封面标题，不再默认生成无字封面，也不为此暂停请求确认。

用户泛指“封面不要文字”时，同时应用于头条长封面和分享页方形封面；只有用户明确点名其中一种时，才只对该封面应用无字要求。

除非用户明确要求正文图无文字，或文字会造成事实错误、安全风险或严重失敬，否则每张正文图默认使用 `1–3` 个简短文字锚点，使画面与少量文字共同完成读者任务。文字必须保持从属，不能变成横跨画面的第二个封面标题。

用户可以明确覆盖交付范围、封面文字、正文文字和正文画幅。正文图默认 `16:9`；仅当用户明确指定时，才可改为 `4:3` 或 `3:4`。未明确覆盖时，不得静默省略方形分享封面、把正文图改成其他比例，或交付没有文字锚点的正文图。

### 图文一体硬约束

- 画面、文字容器和容器内文字必须由图像生成模型在同一次生成中完成。
- 计划列出的标题、标签、数字和单位都是必画对象，不是可选建议。最终图不得留下本应有字的空标签。
- 所有文字必须像同场制作和拍摄的厚毡切字、薄毡贴字或刺绣字，继承画面的纤维、厚度、光向和阴影。
- 禁止使用本地代码、字体渲染、图层合成、矢量工具或其他外部排字方式补写文字。
- 错字、漏字、空标签、额外字符、乱码、错配容器或平面字体均为硬失败。只能继续使用图像生成模型定向编辑或整图重生成；不得交付失败版本。
- 正文短词不得因为字少而自动放大。宁可减少标签数量或缩短标签，也不要让正文文字抢走主视觉。

### 真实羊毛毛毡硬约束

- 最大色块在 100% 原图下必须能看见密集、细短、断续、方向随机且互相交错的离散羊毛纤维。
- 主体必须由平整厚毡裁片分层叠放；至少一处可见毡片厚度、纤维切面、轻微毛边和短软接触阴影。
- 锁边针脚必须是独立线材，具有真实穿刺点并跨过或包住毛毡切边。
- 皮革、麂皮、仿皮、压花革、雕花革、皮革粒面、乙烯基、橡胶、连续卷曲压纹、油边、滚边、管状包边、鞍缝凹槽和窄亮倒角均为硬失败。
- 文字只能是单独裁切的薄毛毡贴字或真实刺绣线迹；凹刻、压印、烫印和皮革压字均为硬失败。

用户的明确要求高于默认值。除非会造成事实错误、安全或严重失敬，否则不要用风格规则否定用户选择。
图文一体、禁用外部排字与禁止空标签属于本 Skill 的执行边界，不是可被覆盖的风格默认；调用本 Skill 时始终生效。

## 必读资源

- 规划前完整读取 [内容路由与选点](references/content-strategy.md)。
- 选择画面对象时读取 [3D 毛毡视觉资产库](references/asset-library.md)。
- 写提示词前读取 [3D 毛毡视觉系统](references/material-dna.md) 与 [提示词模板](references/prompt-template.md)。
- 每张生成后读取并执行 [质量检查](references/qa-checklist.md)。
- `assets/ai-text-flow-reference.png` 是用户认可的真实羊毛毛毡工艺与核心阶段标签标尺。只继承离散短纤维、厚毡裁片、锁边缝线、亚麻背景、标签尺度和适中文字比例；不复制其主题、对象、文字或构图。

执行时先解析两个绝对路径：

- `skill_root`：当前 `SKILL.md` 所在目录；
- `output_dir`：本次产物目录。

后续参考图、计划、提示词、质检与成图都使用绝对路径，不假设当前终端位于 Skill 或输出目录。

## 解析交付范围

| 用户表达 | 交付 |
|---|---|
| 只要封面 | 1 张头条长封面 + 1 张方形分享页封面 |
| 只要长封面 / 头条封面 | 1 张头条长封面 |
| 只要分享封面 / 方形封面 | 1 张方形分享页封面 |
| 只要 N 张配图 / 正文图 | 恰好 N 张正文配图 |
| 封面 + N 张配图 | 1 张头条长封面 + 1 张方形分享页封面 + N 张正文配图 |
| 只说“生成 N 张图”，无法判断是否含封面 | 先确认 N 是总数还是正文图数 |
| 明确说“生成几张正文图”，未给数量 | 推荐数量并只生成正文图 |
| 要求整套视觉、未指定范围 | 1 张头条长封面 + 1 张方形分享页封面 + 推荐数量正文图 |
| 只问数量、方案或配图建议 | 只回复方案，不写文件、不调用生图工具 |

不要因为调用本 Skill 就修改原文或插回图片链接。只有用户明确要求时才改文章。

用户指定数量明显超过非重复优质选点时，先给出可保证质量的最大数量并请求选择；不要用装饰图凑数。用户要求少量图片时，只生成指定数量。

## 工作流

### 1. 完整读取与盘点

读取标题、正文、现有图片、图注和用户约束。明确记录标题状态：

- **原文标题**：文章明确提供的标题；
- **推导标题**：文章没有标题，由核心命题和读者承诺忠实提炼；
- **无字封面**：仅在用户明确要求对应封面不要文字时使用。

文件来源保持不变；粘贴正文默认不另存副本，除非用户要求留档。

记录已有截图、图表或照片，避免重复解释。遇到精确软件界面、代码、地图、医学结构或数据图时，优先保留真实截图或正规图表；不要用毛毡伪造像素级事实。

### 2. 做文章路由

依据 [内容路由与选点](references/content-strategy.md) 判断：

- 文章体裁与混合比例；
- 表层主题、深层诉求和按主次排列的 1–4 个核心概念；
- 一句话核心命题；
- 读者最终需要理解、区分、完成、感受或记住什么；
- 情绪基调和适合的色温；
- 严肃度与风险；
- 哪些位置适合场景、结构图、通用符号或隐喻；
- 哪些位置不应配毛毡图。

灾难伤亡、沉重历史、法律判决和真实创伤等题材不得套用可爱、玩具化表现。若毛毡本身会显得失敬，停止生成并说明；仍适合时切换为克制严肃模式。

### 3. 选点与确定数量

每张正文图都要有独立“读者任务”，例如看清流程、区分概念、理解数字或因果、学会动作、看见人物转折或记住关键体验。

不按章节平均分配，不机械照画修辞，不生成纯装饰图。数量由合格选点决定，篇幅只提供软上限。

### 4. 建立可追溯方案

实际生成前保存 `plan.md`。先记录整篇：

```text
文章类型:
标题状态: 原文标题 | 推导标题 | 用户指定无字
头条长封面标题:
分享页封面标题:
标题依据: 原文标题，或支撑推导标题的核心命题与原句
表层主题:
深层诉求:
核心概念: 1–4 个，按主次排列
核心命题:
读者总任务:
情绪基调:
风险等级:
系列色板与工艺:
正文默认画幅: 16:9，或用户明确指定的比例
推荐交付:
```

每张图记录：

```text
角色: 头条长封面 | 分享页封面 | 正文图
放置位置: 封面用途，或“放在「{原文完整句子}」之后”
原文锚点: 可在原文中逐字搜索到的最短唯一完整句子；封面写标题依据
读者任务:
视觉模式: 具象场景 | 关系结构 | 通用符号 | 动作隐喻 | 克制几何
核心信息:
主角/主符号:
动作与结果:
文字策略: 原文标题 | 推导标题 | 正文默认少量文字 | 用户指定无字 | 风险例外
必须出现的唯一文字: 按阅读顺序逐项列出；正文默认 1–3 项
每项文字所属容器与固定断行:
文字形态: 厚毡切字 | 薄毡贴字 | 刺绣字 | 无字
文字层级: 封面标题 | 普通辅助标签 | 核心阶段标签 | 微型页签 | 关键数字
文字目标尺寸与硬上限:
无字理由: 仅在用户明确要求或风险例外时填写
禁止出现的文字:
标题/标签与主视觉的关系:
不得新增的语义:
盲测期望复述:
图注与 alt 文本:
文件名:
```

无法写出“盲测期望复述”或必须依赖整段解释才能成立的候选，删除或改成更直接的视觉模式。

### 5. 选择视觉模式

按以下优先级选择，不要默认全部隐喻化：

1. **具象场景**：正文有人物、操作、情绪、生活动作或叙事转折；
2. **关系结构**：步骤、流程、时间线、对比、框架、数据和决策；
3. **通用符号**：单一概念存在无歧义符号，如时钟、日历、靶心、对勾、放大镜；
4. **动作隐喻**：前三项无法准确表达时才使用，并必须通过三秒复述测试；
5. **克制几何**：严肃题材需要降低可爱感时使用。

一张图只承担一个理解任务，但可以包含完成该任务所必需的完整三步流程或三项对比。

生成前做一次“皮带/扣件风险预检”。只要方案同时包含窄长带、圆环、扣件、船锚、连续道路或整圈缝边，并可能被看成皮带、表带、皮带扣或滚边软垫，就在写提示词前改用宽大平整毡片、独立节点、重叠圆片/花瓣或彼此分离的脚印/踏石。不得把“核心使命、成长路径、人生锚点”等抽象概念默认画成长带穿环或船锚挂带。

### 6. 锁定系列视觉

同一篇文章固定底材、纤维尺度、切边、针脚、镜头、光向、阴影、色板、人物简化程度，以及毛毡文字的字重、厚度和标签容器样式。

每张图只把参考图当作材质与正文标签尺度标尺，不把前一张成图当作下一张的布局模板。

### 7. 保存单阶段提示词

每个候选生成前保存完整提示词：

```text
prompts/00-cover-wide-v1.md
prompts/00-cover-square-v1.md
prompts/01-<slug>-v1.md
prompts/01-<slug>-v2.md
```

提示词按“读者任务 → 画面结构 → 主角动作 → 必须出现的唯一文字 → 容器映射 → 图文关系 → 材质 → 色板 → 禁止项”书写。

任何含文字的提示词都必须：

1. 从原文复制文字，不凭记忆重打；
2. 固定断行只能插入换行，不能用换行替代或删除原有逗号、冒号、问号等字符；
3. 用一个独立的“必须渲染的唯一文字”区块逐项列出；
4. 为每项文字指定唯一容器、位置、固定断行和毛毡形态；
5. 明说“这些文字不可省略，所有对应标签必须有字”；
6. 明说“除清单外不生成任何文字、伪文字或乱码”；
7. 在提示词结尾再次逐项重复文字清单，作为生成前自检。

文章没有原标题时，先从核心命题、读者承诺和正文中的高置信原词提炼头条长封面标题，再为方形分享页封面生成更短的同义标题：

- 头条长封面推导标题通常为 `8–16` 个汉字，可使用一个必要标点；
- 分享页标题通常为 `4–10` 个汉字，保留同一核心名词或动作；
- 不添加正文没有的数字、结论、身份、效果承诺、热点词或英文副标题；
- 在 `plan.md` 中记录“推导标题”和支撑它的核心命题与原句；
- 除非用户明确要求对应封面无文字，否则不得改为无字封面，也不为标题确认暂停生成。

正文提示词默认选择 `1–3` 个完成读者任务所需的短文字锚点。具象场景或通用符号图通常使用 `1–2` 项，每项 `2–6` 个汉字；流程、对比或框架图通常使用 `2–3` 项，每项 `2–5` 个汉字。优先逐字使用相邻正文原词；若必须做忠实短称，需在 `plan.md` 标记“推导短称”及其原文依据。只有用户明确要求正文无字或风险例外成立时，才使用“无文字”区块。

### 8. 逐张生成

使用当前运行时的内置位图生成工具；在 Codex 中优先使用 `image_gen`。不得用 SVG、HTML、Canvas 或占位图代替。

- 每个候选版本单独调用一次生图工具；
- 参考图只校准真实羊毛毛毡工艺，以及核心阶段标签外框与适中文字尺度，不提供主题或布局；
- 头条长封面默认约 `2.35:1`，分享页封面固定 `1:1`，正文图默认 `16:9`；用户明确指定时，正文图也支持 `4:3` 或 `3:4`；
- 用户未指定正文比例时，不得按结构改成 `4:3` 或 `3:4`；若信息在 `16:9` 中拥挤，优先减少节点、拆分读者任务或精简标签；
- 同一篇所有正文图统一画幅体系，不随意混用；
- 方形分享页封面是长封面的简化同系列版本：保留同一核心命题、主符号与色板，只使用一个主焦点和更短标题，不把长封面机械裁成正方形；
- 原文标题或推导标题都必须在同一次生成中完成主视觉、标题容器与毛毡标题；
- 仅当用户明确要求对应封面无文字时，才生成无字封面；
- 正文图默认在同一次生成中完成主场景与 `1–3` 个少量毛毡文字锚点；
- 正文含必要标签、数字或单位时，必须在同一次生成中完成主场景、标签容器和容器内毛毡文字；
- 普通辅助标签外框约为画布宽 `14%–18%`、高 `9%–12%`；核心阶段标签或多节点核心牌约宽 `20%–22.2%`、高 `15%–18%`；微型页签约宽 `9.5%–12%`、高 `6%–8%`；
- 470px 宽预览下，普通辅助标签可见字形目标 `11–14px`、硬上限 `15px`；核心阶段标签目标 `16–19px`、硬上限 `20px`；微型页签目标 `8–10px`、硬上限 `11px`；关键数字或步骤号目标 `12–16px`、硬上限 `18px`；
- 核心阶段标签适合单行 `3–4` 个汉字；超过 `4` 个字优先缩短，必要时两行且每行目标 `11–13px`。非 16:9 画幅优先保持标签约 `2.1:1–2.3:1` 的长宽比，以 470px 预览标尺为准，不机械套用高度百分比；
- 文字块宽不得超过画布 `20%`；普通辅助标签与微型页签的字面宽不超过容器宽 `72%`，核心阶段标签不超过 `78%`；所有层级字面高不超过容器高 `55%`。三块核心阶段标签总面积不得超过画布 `12%`；
- 容器内文字必须光学居中，四周留白均衡，不贴边、不溢出、不被针脚遮挡。

生成后立刻查看 100% 原图和约 470px 宽缩略图：原图检查离散短纤维、纤维切边和跨边锁边针脚，缩略图检查标签比例与文字可读性，并逐字盲转录。任何计划中的标签为空、材质皮革化或文字低于对应可读下限都立即失败。

### 9. 只用 AI 迭代

每次失败只修最低分项：

1. 首次生成；
2. 仅文字失败时，用图像生成模型定向编辑，保持其余画面不变；
3. 第二次仍有文字问题时，简化标签数量、缩短标签或改成更清楚的整体构图后重生成整图；
4. 仍失败时再尝试一个更直接的视觉模式；
5. 达到 4 次仍失败则停止该图并说明，不交付空标签或错误文字。

语义失败优先从隐喻退回通用符号、关系结构或具象场景。禁止以删除计划中的必要文字来规避失败。

### 10. 盲测与质检

每张图必须实际查看原图和手机缩略图，并执行 [质量检查](references/qa-checklist.md)。

盲测者只能看到成图；第一次不提供提示词和正文，第二次最多提供相邻小标题。若仍不能正确复述主角、动作和核心关系，判定失败。

含文字图必须把期望文字、盲转录、逐项比对、毛毡材质证据、容器居中和缩略图可读性写入 `qa/text-verification.md`。必须零错字、零漏字、零空标签、零额外字。

两种封面另行核对标题来源与一致性：推导标题必须能回指核心命题和原句；方形标题可以缩短，但不得改变承诺或主题。正文图另行核对默认文字覆盖与画幅：默认必须为 `16:9`，仅用户明确指定时可使用 `4:3` 或 `3:4`；若无字或使用其他比例，必须存在用户明确要求或风险例外记录。

逐项验证所有正文插入锚点：锚点必须是从原文逐字复制的完整句子，能在原文中唯一搜索到，且图的读者任务确实解释该句或紧随其后的内容。不得只写“第二部分后”“合适位置”或改写后的概述。

### 11. 保存与交付

默认相对于当前工作目录输出：

```text
felt-illustrations/<article-slug>/
├── plan.md
├── placement-guide.md
├── prompts/
├── qa/
│   └── text-verification.md
├── drafts/
├── 00-cover-wide.png
├── 00-cover-square.png
└── 01-<slug>.png
```

已有同名目录时追加版本号，不覆盖用户文件。低于门槛的版本只能放入 `drafts/`，不得混入最终文件。

`placement-guide.md` 使用以下结构，所有正文终稿按文件名顺序逐项填写：

```text
## 01-<slug>.png
所属小节: <原文小节标题；无小节时写“正文”>
插入提示: 将 `01-<slug>.png` 放在「{原文完整句子}」之后。
读者任务: <这张图帮助读者更快理解什么>
图中文字: <按阅读顺序列出>
```

交付时简洁说明：

- 实际数量与范围；
- 两种封面的用途、标题来源与简化关系；
- 每张正文图对应位置和读者任务，并逐项直接写出“将 `{文件名}` 放在「{原文完整句子}」之后。”；
- 最终图片、`plan.md`、提示词与 `qa/text-verification.md`；
- `placement-guide.md`，其中逐项列出文件名、原文完整句子、所属小节和读者任务；
- 哪些图经过定向编辑或重生成，以及修正了什么。
